@model IEnumerable<WebApplication4.Speciality>

@{
    ViewBag.Title = "Index";
}

<h2>Специальности</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSpeciality }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSpeciality }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSpeciality })
        </td>
    </tr>
}

</table>
